## PVST 思科私有多实例生成树
PVST是Cisco的生成树私有协议，为解决传统STP单实例，维护的生成树过大的性能问题。
它的核心思想是：为网络中的每个VLAN独立运行一个生成树实例，使得不同VLAN流量可以走不同的无环路径，实现负载均衡。
在CST公共生成树中，一处变动会导致整个生成树网络重新收敛，多实例生成树使得每个实例单独收敛，减少网络动荡。

### BID构成
* 在802.1D中，BID构成为 **优先级 + 桥MAC**
* 在PVST中，BID构成为 **(优先级+VLAN ID) + 桥MAC**

